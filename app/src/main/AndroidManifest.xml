<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ec.klecet.com.klecetbustrack">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".LocationUpdates" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".BusInfo"
            android:label="@string/title_activity_bus_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".B2MapsActivity"
            android:label="@string/title_activity_b2_maps" />
        <activity
            android:name=".B1MapsActivity"
            android:label="@string/title_activity_b1_maps" />
        <activity
            android:name=".B3MapsActivity"
            android:label="@string/title_activity_b3_maps" />
        <activity
            android:name=".B4MapsActivity"
            android:label="@string/title_activity_b4_maps" />
        <activity
            android:name=".B5MapsActivity"
            android:label="@string/title_activity_b5_maps" />
        <activity
            android:name=".B6MapsActivity"
            android:label="@string/title_activity_b6_maps" />
        <activity android:name=".SelectBus" />
        <activity
            android:name=".AllBusMapsActivity"
            android:label="@string/title_activity_all_bus_maps"></activity>
    </application>

</manifest>